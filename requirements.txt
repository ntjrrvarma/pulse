# Core ML and data
torch
numpy>=1.24
pandas>=2.0
scikit-learn>=1.2
matplotlib>=3.7
tqdm>=4.65

# Streamlit UI
streamlit>=1.24
plotly>=5.15

# API and serving
fastapi>=0.95
uvicorn[standard]>=0.22

# Utilities
joblib>=1.2
python-multipart>=0.0.6

# NOTE: Install `torch` separately if you require a specific CUDA build.
# Example (CPU-only): `pip install torch --index-url https://download.pytorch.org/whl/cpu`